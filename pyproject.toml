[project]
name = "database-converter"
version = "0.1.0"
description = "Convert SQLite databases to Excel with one sheet per table"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
]
